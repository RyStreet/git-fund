import React from 'react';
// import { useQuery } from '@apollo/client';

function Homepage() {
  return (
    <div>
      <h1>Homepage</h1>
      {/* <ProjectCard /> */}
    </div>
  )
};

export default Homepage;